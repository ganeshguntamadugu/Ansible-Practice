- name: Functions or Filters
  hosts: local
  connection: local
  tasks:
    - name: Printing the default Variable
      ansible.builtin.debug:
        msg: "Hello {{peru | default('Ganesh')}}"
    - name: Printing the default Variable
      ansible.builtin.debug:
        msg: "Hello {{age | default(24)}}"